# ระบบแจ้งเตือนในเว็บ - คำอธิบาย

## ระบบแจ้งเตือนที่มีอยู่แล้ว

### 1. **Telegram Alerts** ✅
- **เมื่อไหร่**: เมื่อ domain ถูก BLOCKED
- **ส่งไปที่**: Telegram Bot (ที่ตั้งค่าไว้)
- **ข้อความ**: 
  ```
  🚨 DOMAIN ALERT 🚨
  Domain: example.com
  Status: BLOCKED / UNREACHABLE
  Detected on: AIS, DTAC
  ```

### 2. **System Logs** ✅
- **แสดงที่**: ด้านขวาของหน้าเว็บ (SYSTEM LOGS)
- **ประเภท**:
  - `alert` - สีแดง, ตัวหนา, animate-pulse (เมื่อ domain ถูก block)
  - `error` - สีแดง (เมื่อเกิด error)
  - `success` - สีเขียว (เมื่อสำเร็จ)
  - `info` - สีขาว (ข้อมูลทั่วไป)

### 3. **Visual Indicators** ✅
- **Domain Cards**: แสดงสถานะของแต่ละ ISP
  - 🟢 ACTIVE (เขียว)
  - 🔴 BLOCKED (แดง)
  - 🟡 ERROR (เหลือง)
  - 🔵 PENDING (น้ำเงิน, หมุน)

---

## การทำงานของระบบแจ้งเตือน

### เมื่อ Domain ถูก Block:

1. **ตรวจพบ**: ระบบตรวจพบว่า domain ถูก block
2. **Log Alert**: เพิ่ม log ใน SYSTEM LOGS (สีแดง, animate-pulse)
3. **Telegram Alert**: ส่งไป Telegram (ถ้าตั้งค่าไว้)
4. **Visual Update**: Domain card แสดงสถานะ BLOCKED

### ตัวอย่าง Log:
```
🚨 ufathai.win BLOCKED on AIS, DTAC
✅ Telegram alert sent to default chat
```

---

## วิธีตั้งค่า Telegram Alerts

### 1. สร้าง Telegram Bot
1. เปิด Telegram → ค้นหา `@BotFather`
2. ส่ง `/newbot`
3. ตั้งชื่อ bot
4. รับ **Bot Token** (เช่น: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. หา Chat ID
1. ส่งข้อความไปที่ bot ที่สร้าง
2. เปิด: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. หา `"chat":{"id":123456789}` → นี่คือ Chat ID

### 3. ตั้งค่าในเว็บ
1. เปิดหน้าเว็บ → **Settings**
2. กรอก **Telegram Bot Token**
3. กรอก **Telegram Chat ID**
4. กด **Save**

---

## การแจ้งเตือนจาก Mobile App

เมื่อ mobile app sync ผลลัพธ์มา:
- ระบบจะตรวจสอบสถานะใหม่
- ถ้าเจอ BLOCKED → ส่ง Telegram alert อัตโนมัติ
- แสดงใน SYSTEM LOGS

---

## ข้อเสนอแนะสำหรับการปรับปรุง

### 1. Browser Notifications (Web Notifications API)
- แจ้งเตือนผ่าน browser แม้ไม่ได้เปิดหน้าเว็บ
- ต้องขอ permission จากผู้ใช้

### 2. Sound Alerts
- เสียงเตือนเมื่อ domain ถูก block
- ปรับ volume ได้

### 3. Email Alerts
- ส่ง email เมื่อ domain ถูก block
- ใช้บริการเช่น SendGrid, Mailgun

### 4. Desktop Notifications
- แจ้งเตือนผ่าน desktop (ถ้าใช้ PWA)

---

## สรุป

### ตอนนี้มี:
- ✅ Telegram alerts (เมื่อ domain ถูก block)
- ✅ System logs (แสดงในหน้าเว็บ)
- ✅ Visual indicators (สี/ไอคอน)

### ต้องการเพิ่ม:
- ❓ Browser notifications?
- ❓ Sound alerts?
- ❓ Email alerts?
- ❓ อื่นๆ?

---

## หมายเหตุ

- **Telegram alerts** ทำงานเฉพาะเมื่อ domain ถูก **BLOCKED**
- **System logs** แสดงทุก event (info, success, error, alert)
- **Visual indicators** อัพเดทแบบ real-time


## ระบบแจ้งเตือนที่มีอยู่แล้ว

### 1. **Telegram Alerts** ✅
- **เมื่อไหร่**: เมื่อ domain ถูก BLOCKED
- **ส่งไปที่**: Telegram Bot (ที่ตั้งค่าไว้)
- **ข้อความ**: 
  ```
  🚨 DOMAIN ALERT 🚨
  Domain: example.com
  Status: BLOCKED / UNREACHABLE
  Detected on: AIS, DTAC
  ```

### 2. **System Logs** ✅
- **แสดงที่**: ด้านขวาของหน้าเว็บ (SYSTEM LOGS)
- **ประเภท**:
  - `alert` - สีแดง, ตัวหนา, animate-pulse (เมื่อ domain ถูก block)
  - `error` - สีแดง (เมื่อเกิด error)
  - `success` - สีเขียว (เมื่อสำเร็จ)
  - `info` - สีขาว (ข้อมูลทั่วไป)

### 3. **Visual Indicators** ✅
- **Domain Cards**: แสดงสถานะของแต่ละ ISP
  - 🟢 ACTIVE (เขียว)
  - 🔴 BLOCKED (แดง)
  - 🟡 ERROR (เหลือง)
  - 🔵 PENDING (น้ำเงิน, หมุน)

---

## การทำงานของระบบแจ้งเตือน

### เมื่อ Domain ถูก Block:

1. **ตรวจพบ**: ระบบตรวจพบว่า domain ถูก block
2. **Log Alert**: เพิ่ม log ใน SYSTEM LOGS (สีแดง, animate-pulse)
3. **Telegram Alert**: ส่งไป Telegram (ถ้าตั้งค่าไว้)
4. **Visual Update**: Domain card แสดงสถานะ BLOCKED

### ตัวอย่าง Log:
```
🚨 ufathai.win BLOCKED on AIS, DTAC
✅ Telegram alert sent to default chat
```

---

## วิธีตั้งค่า Telegram Alerts

### 1. สร้าง Telegram Bot
1. เปิด Telegram → ค้นหา `@BotFather`
2. ส่ง `/newbot`
3. ตั้งชื่อ bot
4. รับ **Bot Token** (เช่น: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. หา Chat ID
1. ส่งข้อความไปที่ bot ที่สร้าง
2. เปิด: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. หา `"chat":{"id":123456789}` → นี่คือ Chat ID

### 3. ตั้งค่าในเว็บ
1. เปิดหน้าเว็บ → **Settings**
2. กรอก **Telegram Bot Token**
3. กรอก **Telegram Chat ID**
4. กด **Save**

---

## การแจ้งเตือนจาก Mobile App

เมื่อ mobile app sync ผลลัพธ์มา:
- ระบบจะตรวจสอบสถานะใหม่
- ถ้าเจอ BLOCKED → ส่ง Telegram alert อัตโนมัติ
- แสดงใน SYSTEM LOGS

---

## ข้อเสนอแนะสำหรับการปรับปรุง

### 1. Browser Notifications (Web Notifications API)
- แจ้งเตือนผ่าน browser แม้ไม่ได้เปิดหน้าเว็บ
- ต้องขอ permission จากผู้ใช้

### 2. Sound Alerts
- เสียงเตือนเมื่อ domain ถูก block
- ปรับ volume ได้

### 3. Email Alerts
- ส่ง email เมื่อ domain ถูก block
- ใช้บริการเช่น SendGrid, Mailgun

### 4. Desktop Notifications
- แจ้งเตือนผ่าน desktop (ถ้าใช้ PWA)

---

## สรุป

### ตอนนี้มี:
- ✅ Telegram alerts (เมื่อ domain ถูก block)
- ✅ System logs (แสดงในหน้าเว็บ)
- ✅ Visual indicators (สี/ไอคอน)

### ต้องการเพิ่ม:
- ❓ Browser notifications?
- ❓ Sound alerts?
- ❓ Email alerts?
- ❓ อื่นๆ?

---

## หมายเหตุ

- **Telegram alerts** ทำงานเฉพาะเมื่อ domain ถูก **BLOCKED**
- **System logs** แสดงทุก event (info, success, error, alert)
- **Visual indicators** อัพเดทแบบ real-time


## ระบบแจ้งเตือนที่มีอยู่แล้ว

### 1. **Telegram Alerts** ✅
- **เมื่อไหร่**: เมื่อ domain ถูก BLOCKED
- **ส่งไปที่**: Telegram Bot (ที่ตั้งค่าไว้)
- **ข้อความ**: 
  ```
  🚨 DOMAIN ALERT 🚨
  Domain: example.com
  Status: BLOCKED / UNREACHABLE
  Detected on: AIS, DTAC
  ```

### 2. **System Logs** ✅
- **แสดงที่**: ด้านขวาของหน้าเว็บ (SYSTEM LOGS)
- **ประเภท**:
  - `alert` - สีแดง, ตัวหนา, animate-pulse (เมื่อ domain ถูก block)
  - `error` - สีแดง (เมื่อเกิด error)
  - `success` - สีเขียว (เมื่อสำเร็จ)
  - `info` - สีขาว (ข้อมูลทั่วไป)

### 3. **Visual Indicators** ✅
- **Domain Cards**: แสดงสถานะของแต่ละ ISP
  - 🟢 ACTIVE (เขียว)
  - 🔴 BLOCKED (แดง)
  - 🟡 ERROR (เหลือง)
  - 🔵 PENDING (น้ำเงิน, หมุน)

---

## การทำงานของระบบแจ้งเตือน

### เมื่อ Domain ถูก Block:

1. **ตรวจพบ**: ระบบตรวจพบว่า domain ถูก block
2. **Log Alert**: เพิ่ม log ใน SYSTEM LOGS (สีแดง, animate-pulse)
3. **Telegram Alert**: ส่งไป Telegram (ถ้าตั้งค่าไว้)
4. **Visual Update**: Domain card แสดงสถานะ BLOCKED

### ตัวอย่าง Log:
```
🚨 ufathai.win BLOCKED on AIS, DTAC
✅ Telegram alert sent to default chat
```

---

## วิธีตั้งค่า Telegram Alerts

### 1. สร้าง Telegram Bot
1. เปิด Telegram → ค้นหา `@BotFather`
2. ส่ง `/newbot`
3. ตั้งชื่อ bot
4. รับ **Bot Token** (เช่น: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. หา Chat ID
1. ส่งข้อความไปที่ bot ที่สร้าง
2. เปิด: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. หา `"chat":{"id":123456789}` → นี่คือ Chat ID

### 3. ตั้งค่าในเว็บ
1. เปิดหน้าเว็บ → **Settings**
2. กรอก **Telegram Bot Token**
3. กรอก **Telegram Chat ID**
4. กด **Save**

---

## การแจ้งเตือนจาก Mobile App

เมื่อ mobile app sync ผลลัพธ์มา:
- ระบบจะตรวจสอบสถานะใหม่
- ถ้าเจอ BLOCKED → ส่ง Telegram alert อัตโนมัติ
- แสดงใน SYSTEM LOGS

---

## ข้อเสนอแนะสำหรับการปรับปรุง

### 1. Browser Notifications (Web Notifications API)
- แจ้งเตือนผ่าน browser แม้ไม่ได้เปิดหน้าเว็บ
- ต้องขอ permission จากผู้ใช้

### 2. Sound Alerts
- เสียงเตือนเมื่อ domain ถูก block
- ปรับ volume ได้

### 3. Email Alerts
- ส่ง email เมื่อ domain ถูก block
- ใช้บริการเช่น SendGrid, Mailgun

### 4. Desktop Notifications
- แจ้งเตือนผ่าน desktop (ถ้าใช้ PWA)

---

## สรุป

### ตอนนี้มี:
- ✅ Telegram alerts (เมื่อ domain ถูก block)
- ✅ System logs (แสดงในหน้าเว็บ)
- ✅ Visual indicators (สี/ไอคอน)

### ต้องการเพิ่ม:
- ❓ Browser notifications?
- ❓ Sound alerts?
- ❓ Email alerts?
- ❓ อื่นๆ?

---

## หมายเหตุ

- **Telegram alerts** ทำงานเฉพาะเมื่อ domain ถูก **BLOCKED**
- **System logs** แสดงทุก event (info, success, error, alert)
- **Visual indicators** อัพเดทแบบ real-time

