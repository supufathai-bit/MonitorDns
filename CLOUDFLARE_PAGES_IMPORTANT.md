# ⚠️ สำคัญ: Cloudflare Pages Setup

## 🔴 ปัญหาปัจจุบัน

1. **Build Output Directory ผิด**
   - ตอนนี้: `.next` (ไม่ถูกต้อง)
   - ต้องเป็น: `out` (สำหรับ static export)

2. **API Routes ไม่ทำงาน**
   - API routes (`/api/*`) ไม่ทำงานใน static export
   - ต้องใช้ Cloudflare Workers แทน

---

## ✅ วิธีแก้ไข

### 1. เปลี่ยน Build Output Directory

**ใน Cloudflare Pages Settings:**

1. ไปที่ **Settings** → **Builds & deployments**
2. เปลี่ยน **Build output directory** จาก `.next` → `out`
3. **Save**

### 2. ตรวจสอบ Build

หลังจากเปลี่ยนแล้ว:
- Cloudflare จะ rebuild อัตโนมัติ
- หรือกด **Retry deployment**

---

## 📋 การตั้งค่าที่ถูกต้อง

| Field | Value |
|-------|-------|
| **Framework preset** | `Next.js` |
| **Build command** | `npm run build` |
| **Build output directory** | `out` ⚠️ **สำคัญ** |
| **Root directory** | (ว่าง) |

---

## 🚨 หมายเหตุ

- **ต้องเปลี่ยน Build output directory เป็น `out`**
- ถ้าไม่เปลี่ยน จะได้ 404 error
- `out` folder จะถูกสร้างเมื่อ `npm run build` (static export)

---

## ✅ Checklist

- [ ] เปลี่ยน Build output directory เป็น `out`
- [ ] Retry deployment
- [ ] ตรวจสอบว่า build สำเร็จ
- [ ] ตรวจสอบว่าเว็บทำงาน

---

## 🔍 ตรวจสอบ

หลังจาก deploy:
- เปิด `https://monitordns.pages.dev`
- ควรเห็นหน้าเว็บ (ไม่ใช่ 404)

---

## 💡 ถ้ายังไม่ได้

1. ตรวจสอบ build logs ใน Cloudflare
2. ตรวจสอบว่า `out` folder ถูกสร้าง
3. ตรวจสอบว่าไม่มี error ใน build

