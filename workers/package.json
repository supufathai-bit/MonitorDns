{
  "name": "sentinel-dns-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for Sentinel DNS Monitor",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240101.0",
    "wrangler": "^3.19.0"
  }
}

