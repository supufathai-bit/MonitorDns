# สถานะการเชื่อมต่อ Telegram

## ✅ ระบบเชื่อมต่อ Telegram ได้

### เงื่อนไขการทำงาน:
1. **ต้องตั้งค่า Telegram Bot Token** ใน Settings
2. **ต้องตั้งค่า Telegram Chat ID** ใน Settings
3. **จะส่งแจ้งเตือนเมื่อ**: Domain ถูก **BLOCKED** เท่านั้น

---

## 🔍 ปัญหาที่เป็นไปได้

### 1. ยังไม่ได้ตั้งค่า Telegram
- **ตรวจสอบ**: เปิด Settings → ดู Telegram Bot Token และ Chat ID
- **ถ้าว่าง**: ต้องตั้งค่าก่อน

### 2. ระบบส่งแจ้งเตือนเฉพาะเมื่อ BLOCKED
- **ไม่ส่งเมื่อ**: Status เป็น ERROR, PENDING, หรือ ACTIVE
- **ส่งเมื่อ**: Status เป็น BLOCKED เท่านั้น

### 3. ระบบตรวจสอบจาก Mobile App Results
- **ตอนนี้**: ระบบใช้ผลลัพธ์จาก Mobile App
- **ถ้า Mobile App ไม่ sync**: จะไม่มีข้อมูลใหม่ → ไม่ส่งแจ้งเตือน

---

## 📋 วิธีตรวจสอบ

### 1. ตรวจสอบว่า Telegram ตั้งค่าหรือยัง
```javascript
// เปิด Console (F12) แล้วพิมพ์:
const settings = JSON.parse(localStorage.getItem('sentinel_settings'));
console.log('Telegram Bot Token:', settings?.telegramBotToken ? '✅ Set' : '❌ Not Set');
console.log('Telegram Chat ID:', settings?.telegramChatId ? '✅ Set' : '❌ Not Set');
```

### 2. ทดสอบการส่ง Telegram
```bash
# ใช้ test script
node test-telegram.js
```

### 3. ดู Logs ในหน้าเว็บ
- เปิด **SYSTEM LOGS** (ด้านขวา)
- ดูว่ามี log:
  - `✅ Telegram alert sent to default chat` = ส่งสำเร็จ
  - `❌ Failed to send Telegram alert` = ส่งไม่สำเร็จ

---

## 🔧 วิธีแก้ไข

### ถ้ายังไม่ได้ตั้งค่า Telegram:

1. **สร้าง Telegram Bot**:
   - เปิด Telegram → ค้นหา `@BotFather`
   - ส่ง `/newbot`
   - ตั้งชื่อ bot
   - รับ **Bot Token**

2. **หา Chat ID**:
   - ส่งข้อความไปที่ bot ที่สร้าง
   - เปิด: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - หา `"chat":{"id":123456789}` → นี่คือ Chat ID

3. **ตั้งค่าในเว็บ**:
   - เปิดหน้าเว็บ → **Settings**
   - กรอก **Telegram Bot Token**
   - กรอก **Telegram Chat ID**
   - กด **Save**

### ถ้าตั้งค่าแล้วแต่ไม่ส่ง:

1. **ตรวจสอบว่า Domain ถูก BLOCKED จริงหรือไม่**:
   - ดู Domain Cards → ต้องมี ISP ที่แสดง **BLOCKED** (สีแดง)
   - ถ้าเป็น ERROR หรือ PENDING → จะไม่ส่ง

2. **ตรวจสอบ Logs**:
   - ดู SYSTEM LOGS → ต้องมี log `BLOCKED on ...`
   - ถ้าไม่มี → ระบบยังไม่ตรวจพบว่า domain ถูก block

3. **ทดสอบการส่ง**:
   ```bash
   node test-telegram.js
   ```

---

## 📊 สรุป

| สถานะ | การทำงาน |
|-------|----------|
| ✅ **ตั้งค่า Telegram แล้ว** | ส่งแจ้งเตือนเมื่อ domain ถูก BLOCKED |
| ❌ **ยังไม่ได้ตั้งค่า** | ไม่ส่งแจ้งเตือน |
| ⚠️ **Domain เป็น ERROR** | ไม่ส่งแจ้งเตือน (ส่งเฉพาะ BLOCKED) |
| ⚠️ **Mobile App ไม่ sync** | ไม่มีข้อมูลใหม่ → ไม่ส่งแจ้งเตือน |

---

## 💡 หมายเหตุ

- **Telegram alerts** ทำงานเฉพาะเมื่อ domain ถูก **BLOCKED**
- **ไม่ส่ง** เมื่อ status เป็น ERROR, PENDING, หรือ ACTIVE
- **ต้องตั้งค่า** Telegram Bot Token และ Chat ID ก่อนใช้งาน


## ✅ ระบบเชื่อมต่อ Telegram ได้

### เงื่อนไขการทำงาน:
1. **ต้องตั้งค่า Telegram Bot Token** ใน Settings
2. **ต้องตั้งค่า Telegram Chat ID** ใน Settings
3. **จะส่งแจ้งเตือนเมื่อ**: Domain ถูก **BLOCKED** เท่านั้น

---

## 🔍 ปัญหาที่เป็นไปได้

### 1. ยังไม่ได้ตั้งค่า Telegram
- **ตรวจสอบ**: เปิด Settings → ดู Telegram Bot Token และ Chat ID
- **ถ้าว่าง**: ต้องตั้งค่าก่อน

### 2. ระบบส่งแจ้งเตือนเฉพาะเมื่อ BLOCKED
- **ไม่ส่งเมื่อ**: Status เป็น ERROR, PENDING, หรือ ACTIVE
- **ส่งเมื่อ**: Status เป็น BLOCKED เท่านั้น

### 3. ระบบตรวจสอบจาก Mobile App Results
- **ตอนนี้**: ระบบใช้ผลลัพธ์จาก Mobile App
- **ถ้า Mobile App ไม่ sync**: จะไม่มีข้อมูลใหม่ → ไม่ส่งแจ้งเตือน

---

## 📋 วิธีตรวจสอบ

### 1. ตรวจสอบว่า Telegram ตั้งค่าหรือยัง
```javascript
// เปิด Console (F12) แล้วพิมพ์:
const settings = JSON.parse(localStorage.getItem('sentinel_settings'));
console.log('Telegram Bot Token:', settings?.telegramBotToken ? '✅ Set' : '❌ Not Set');
console.log('Telegram Chat ID:', settings?.telegramChatId ? '✅ Set' : '❌ Not Set');
```

### 2. ทดสอบการส่ง Telegram
```bash
# ใช้ test script
node test-telegram.js
```

### 3. ดู Logs ในหน้าเว็บ
- เปิด **SYSTEM LOGS** (ด้านขวา)
- ดูว่ามี log:
  - `✅ Telegram alert sent to default chat` = ส่งสำเร็จ
  - `❌ Failed to send Telegram alert` = ส่งไม่สำเร็จ

---

## 🔧 วิธีแก้ไข

### ถ้ายังไม่ได้ตั้งค่า Telegram:

1. **สร้าง Telegram Bot**:
   - เปิด Telegram → ค้นหา `@BotFather`
   - ส่ง `/newbot`
   - ตั้งชื่อ bot
   - รับ **Bot Token**

2. **หา Chat ID**:
   - ส่งข้อความไปที่ bot ที่สร้าง
   - เปิด: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - หา `"chat":{"id":123456789}` → นี่คือ Chat ID

3. **ตั้งค่าในเว็บ**:
   - เปิดหน้าเว็บ → **Settings**
   - กรอก **Telegram Bot Token**
   - กรอก **Telegram Chat ID**
   - กด **Save**

### ถ้าตั้งค่าแล้วแต่ไม่ส่ง:

1. **ตรวจสอบว่า Domain ถูก BLOCKED จริงหรือไม่**:
   - ดู Domain Cards → ต้องมี ISP ที่แสดง **BLOCKED** (สีแดง)
   - ถ้าเป็น ERROR หรือ PENDING → จะไม่ส่ง

2. **ตรวจสอบ Logs**:
   - ดู SYSTEM LOGS → ต้องมี log `BLOCKED on ...`
   - ถ้าไม่มี → ระบบยังไม่ตรวจพบว่า domain ถูก block

3. **ทดสอบการส่ง**:
   ```bash
   node test-telegram.js
   ```

---

## 📊 สรุป

| สถานะ | การทำงาน |
|-------|----------|
| ✅ **ตั้งค่า Telegram แล้ว** | ส่งแจ้งเตือนเมื่อ domain ถูก BLOCKED |
| ❌ **ยังไม่ได้ตั้งค่า** | ไม่ส่งแจ้งเตือน |
| ⚠️ **Domain เป็น ERROR** | ไม่ส่งแจ้งเตือน (ส่งเฉพาะ BLOCKED) |
| ⚠️ **Mobile App ไม่ sync** | ไม่มีข้อมูลใหม่ → ไม่ส่งแจ้งเตือน |

---

## 💡 หมายเหตุ

- **Telegram alerts** ทำงานเฉพาะเมื่อ domain ถูก **BLOCKED**
- **ไม่ส่ง** เมื่อ status เป็น ERROR, PENDING, หรือ ACTIVE
- **ต้องตั้งค่า** Telegram Bot Token และ Chat ID ก่อนใช้งาน


## ✅ ระบบเชื่อมต่อ Telegram ได้

### เงื่อนไขการทำงาน:
1. **ต้องตั้งค่า Telegram Bot Token** ใน Settings
2. **ต้องตั้งค่า Telegram Chat ID** ใน Settings
3. **จะส่งแจ้งเตือนเมื่อ**: Domain ถูก **BLOCKED** เท่านั้น

---

## 🔍 ปัญหาที่เป็นไปได้

### 1. ยังไม่ได้ตั้งค่า Telegram
- **ตรวจสอบ**: เปิด Settings → ดู Telegram Bot Token และ Chat ID
- **ถ้าว่าง**: ต้องตั้งค่าก่อน

### 2. ระบบส่งแจ้งเตือนเฉพาะเมื่อ BLOCKED
- **ไม่ส่งเมื่อ**: Status เป็น ERROR, PENDING, หรือ ACTIVE
- **ส่งเมื่อ**: Status เป็น BLOCKED เท่านั้น

### 3. ระบบตรวจสอบจาก Mobile App Results
- **ตอนนี้**: ระบบใช้ผลลัพธ์จาก Mobile App
- **ถ้า Mobile App ไม่ sync**: จะไม่มีข้อมูลใหม่ → ไม่ส่งแจ้งเตือน

---

## 📋 วิธีตรวจสอบ

### 1. ตรวจสอบว่า Telegram ตั้งค่าหรือยัง
```javascript
// เปิด Console (F12) แล้วพิมพ์:
const settings = JSON.parse(localStorage.getItem('sentinel_settings'));
console.log('Telegram Bot Token:', settings?.telegramBotToken ? '✅ Set' : '❌ Not Set');
console.log('Telegram Chat ID:', settings?.telegramChatId ? '✅ Set' : '❌ Not Set');
```

### 2. ทดสอบการส่ง Telegram
```bash
# ใช้ test script
node test-telegram.js
```

### 3. ดู Logs ในหน้าเว็บ
- เปิด **SYSTEM LOGS** (ด้านขวา)
- ดูว่ามี log:
  - `✅ Telegram alert sent to default chat` = ส่งสำเร็จ
  - `❌ Failed to send Telegram alert` = ส่งไม่สำเร็จ

---

## 🔧 วิธีแก้ไข

### ถ้ายังไม่ได้ตั้งค่า Telegram:

1. **สร้าง Telegram Bot**:
   - เปิด Telegram → ค้นหา `@BotFather`
   - ส่ง `/newbot`
   - ตั้งชื่อ bot
   - รับ **Bot Token**

2. **หา Chat ID**:
   - ส่งข้อความไปที่ bot ที่สร้าง
   - เปิด: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - หา `"chat":{"id":123456789}` → นี่คือ Chat ID

3. **ตั้งค่าในเว็บ**:
   - เปิดหน้าเว็บ → **Settings**
   - กรอก **Telegram Bot Token**
   - กรอก **Telegram Chat ID**
   - กด **Save**

### ถ้าตั้งค่าแล้วแต่ไม่ส่ง:

1. **ตรวจสอบว่า Domain ถูก BLOCKED จริงหรือไม่**:
   - ดู Domain Cards → ต้องมี ISP ที่แสดง **BLOCKED** (สีแดง)
   - ถ้าเป็น ERROR หรือ PENDING → จะไม่ส่ง

2. **ตรวจสอบ Logs**:
   - ดู SYSTEM LOGS → ต้องมี log `BLOCKED on ...`
   - ถ้าไม่มี → ระบบยังไม่ตรวจพบว่า domain ถูก block

3. **ทดสอบการส่ง**:
   ```bash
   node test-telegram.js
   ```

---

## 📊 สรุป

| สถานะ | การทำงาน |
|-------|----------|
| ✅ **ตั้งค่า Telegram แล้ว** | ส่งแจ้งเตือนเมื่อ domain ถูก BLOCKED |
| ❌ **ยังไม่ได้ตั้งค่า** | ไม่ส่งแจ้งเตือน |
| ⚠️ **Domain เป็น ERROR** | ไม่ส่งแจ้งเตือน (ส่งเฉพาะ BLOCKED) |
| ⚠️ **Mobile App ไม่ sync** | ไม่มีข้อมูลใหม่ → ไม่ส่งแจ้งเตือน |

---

## 💡 หมายเหตุ

- **Telegram alerts** ทำงานเฉพาะเมื่อ domain ถูก **BLOCKED**
- **ไม่ส่ง** เมื่อ status เป็น ERROR, PENDING, หรือ ACTIVE
- **ต้องตั้งค่า** Telegram Bot Token และ Chat ID ก่อนใช้งาน

